---
phase: 01-project-skeleton-deterministic-harness-m0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/sia_sim/__init__.py
  - tests/conftest.py
  - tests/unit/test_skeleton.py
  - ruff.toml
autonomous: true
requirements:
  - SKEL-01
user_setup: []

must_haves:
  truths:
    - "`uv run pytest` runs cleanly with 0 failures"
    - "`uv run ruff check .` passes without errors"
    - "`src/sia_sim` is an importable Python package"
  artifacts:
    - pyproject.toml
    - src/sia_sim/__init__.py
    - tests/conftest.py
    - tests/unit/test_skeleton.py
  key_links:
    - pyproject.toml declares dependencies (pydantic, numpy, scipy, polars, pyarrow, pytest)
---

<objective>
Setup the base Python 3.12+ project skeleton with `uv`, package configuration (`pyproject.toml`), code layout (`src/sia_sim`), linting/type-checking configurations (`ruff`, `mypy`), and a working `pytest` test runner.

Purpose: Provide a modern, clean, reproducible development environment and CI-ready test harness before writing core simulation logic.
Output: Configured repository, package root, basic test harness confirming environment validity.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize uv project and configure pyproject.toml</name>
  <files>pyproject.toml, ruff.toml</files>
  <read_first>.planning/research/STACK.md, .init_docs/SIA-SIM-MVP-001-IMPLEMENTATION PLAN.md</read_first>
  <action>
    Initialize the project using `uv` targeting Python 3.12+.
    Configure `pyproject.toml` with:
    - Project name: `sia-sim`
    - Version: `0.1.0`
    - Requires-python: `>=3.12`
    - Dependencies: `pydantic>=2.10`, `numpy>=2.0`, `scipy>=1.14`, `polars>=1.0`, `pyarrow>=18.0`
    - Dev dependencies: `pytest>=8.0`, `pytest-cov`, `ruff`, `mypy`
    - Package layout pointing to `src/sia_sim`
    - Configure ruff in `pyproject.toml` or `ruff.toml` with strict rules (E, F, W, I, UP, B).
  </action>
  <verify>uv sync</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "sia-sim"`
    - `pyproject.toml` specifies `requires-python = ">=3.12"`
    - `uv sync` installs dependencies without errors
  </acceptance_criteria>
  <done>Python 3.12+ environment locked and dependencies synced via uv.</done>
</task>

<task type="auto">
  <name>Task 2: Create package skeleton and smoke test harness</name>
  <files>src/sia_sim/__init__.py, tests/conftest.py, tests/unit/test_skeleton.py</files>
  <read_first>pyproject.toml</read_first>
  <action>
    1. Create `src/sia_sim/__init__.py` declaring `__version__ = "0.1.0"`.
    2. Create `tests/conftest.py` with shared test helpers/fixtures.
    3. Create `tests/unit/test_skeleton.py` verifying package importability, version check, and dependency availability (pydantic, numpy, polars).
    4. Run `uv run pytest` and verify passing test suite.
  </action>
  <verify>uv run pytest</verify>
  <acceptance_criteria>
    - `src/sia_sim/__init__.py` exists and exposes `__version__`
    - `tests/unit/test_skeleton.py` imports `sia_sim`
    - `uv run pytest` exits with code 0
  </acceptance_criteria>
  <done>Package skeleton is importable and test harness runs cleanly.</done>
</task>

</tasks>
